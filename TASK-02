Task 2: Internet Data Usage Category:

#include <stdio.h>
int main() {
    float data;
    printf("Enter total data used in GB: ");
    scanf("%f", &data);
    if (data <= 50) {
        printf("Basic User\n");
    }
    else if (data <= 150) {
        printf("Standard User\n");
    }
    else {
        printf("Heavy User\n");
    }
    return 0;
}
